"""Qualification MCP domain.

Do NOT auto-import from `.server` here — server.py pulls in `fastmcp`,
which is only needed when the unified MCP server process boots. Importers
that need the tool factory should `from .server import create_server`.
"""
